name: Deploy a Vercel

on:
  push:
    branches: [master, main, SSAAVEDRA, 'claude/**']
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy a Vercel (prebuilt)
        run: npx vercel deploy --prebuilt --token ${{ secrets.VERCEL_TOKEN }} --prod --yes
